# Switch Statement in C++

## Definition
- Switch statement is used for multiple condition checking.
- It is an alternative to long if-else-if ladders.

---

## Syntax

```cpp
switch(expression) {
    case value1:
        // code
        break;

    case value2:
        // code
        break;

    default:
        // code
}